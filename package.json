{
  "name": "actor-quotes",
  "version": "1.0.0",
  "description": "Inspirational quotes from hollywoods actors",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify -t [ babelify --presets [ es2015 ] ] src/client/index.js > dist/app.js",
    "copy": "xcopy /s public dist",
    "serve": "json-server db.json --static ./dist"
  },
  "keywords": [
    "quote"
  ],
  "author": "checholin87@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "grunt": "^0.4.5",
    "grunt-browserify": "^4.0.1",
    "grunt-contrib-copy": "^0.8.2",
    "grunt-run": "^0.5.2"
  },
  "dependencies": {
    "jquery": "^2.1.4"
  }
}
